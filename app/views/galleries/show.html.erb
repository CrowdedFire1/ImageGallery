<%= render @gallery %>

<div>
  <% if !current_user.nil? && current_user.id == @gallery.user_id %>
    <%= link_to "Edit this gallery", edit_gallery_path(@gallery) %> |
    <%= link_to "Back to galleries", galleries_path %>

    <%= button_to "Destroy this gallery", @gallery, method: :delete %>
  <% end %>
</div>
